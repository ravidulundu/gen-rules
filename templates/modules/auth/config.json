{
  "name": "auth",
  "description": "Authentication module with session management (optional tenant support)",
  "dependencies": {
    "arctic": "^2.0.0",
    "oslo": "^1.0.0"
  },
  "files": [
    "src/lib/auth.ts",
    "src/middleware/auth.ts"
  ],
  "options": {
    "tenant": {
      "description": "Enable multi-tenant support with X-Tenant-ID header",
      "default": false
    }
  }
}
